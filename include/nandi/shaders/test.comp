struct Vert {
    vec4 color;
    vec2 position;
    float time;
    float pad;
};

struct Frag {
    vec4 color;
};

Frag vert(Vert vert) {
    Frag frag;
    frag.color = vert.color * (sin(get_global().time * 10) * 0.5f + 0.5f);
    return frag;
}

vec4 frag(vec3 coords, Frag v1, Frag v2, Frag v3) {
    vec4 uv = coords.x * v1.color + coords.y * v2.color + coords.z * v3.color;
    return vec4(uv.x, uv.y, uv.z, 1);
}
